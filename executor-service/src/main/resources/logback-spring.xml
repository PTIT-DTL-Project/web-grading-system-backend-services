<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config for Spring Boot + Micrometer Tracing + OpenTelemetry
  Outputs structured JSON logs with trace_id and span_id for Grafana correlation.

  DROP this file into src/main/resources/logback-spring.xml of each Spring Boot service.
-->
<configuration>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="unknown"/>
    <springProperty scope="context" name="APP_ENV" source="spring.profiles.active" defaultValue="local"/>

    <!-- Console: structured JSON for Loki -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="vn.edu.ptit.web_grading_system.executor_service.config.ReadableLogstashEncoder">
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <includeMdcKeyName>trace_flags</includeMdcKeyName>
            <fieldNames>
                <timestamp>[ignore]</timestamp>
                <version>[ignore]</version>
            </fieldNames>
            <customFields>
                {"service":"${APP_NAME}","environment":"${APP_ENV}"}
            </customFields>
            <timeZone>UTC</timeZone>
        </encoder>
    </appender>

    <!-- FILE appender is local-only (no stg/prod profile): the baked basedir path
         is meaningless inside containers, where only stdout/Loki is used. -->
    <springProfile name="!stg &amp; !prod">
    <!-- File: rolling JSON mirror for local debugging (WAL-style).
         %i is the version number on size rollover. totalSizeCap deletes the
         OLDEST archives first; the active app.log is never touched. -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>@project.basedir@/.log/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>@project.basedir@/.log/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder class="vn.edu.ptit.web_grading_system.executor_service.config.ReadableLogstashEncoder">
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <includeMdcKeyName>trace_flags</includeMdcKeyName>
            <fieldNames>
                <timestamp>[ignore]</timestamp>
                <version>[ignore]</version>
            </fieldNames>
            <customFields>
                {"service":"${APP_NAME}","environment":"${APP_ENV}"}
            </customFields>
            <timeZone>UTC</timeZone>
        </encoder>
    </appender>
    </springProfile>

    <springProfile name="stg | prod">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
    <springProfile name="!stg &amp; !prod">
        <root level="INFO">
            <appender-ref ref="JSON"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

</configuration>
